---
title: "Logical Equivalences"
subtitle: "Lecture 3, {{< var course.code >}}, {{< var course.term >}}"
---

# Propositions Continued

## Parse Trees 

Apply the outermost relevant rule...

::::: columns
::: {.column width="50%" style="text-align: center; font-size: 0.85em; white-space: nowrap;"}
$((\text{Tu} \wedge \text{Th}) \wedge (\text{Sa} \vee \text{Su}))$
:::

::: {.column width="50%" style="text-align: center; font-size: 0.85em; white-space: nowrap;"}
$(\text{Tu} \wedge (\text{Th} \wedge (\text{Sa} \vee \text{Su})))$
:::
:::::


::: notes
Work outward-in: the outermost parentheses are the last rule applied, so they
are the root. Each internal node is a rule application, each leaf an atom.

The trees are **different** -- and every row of the truth table agrees. That gap
is the point. The parentheses fix the structure, the structure fixes how you
evaluate, but two structures can evaluate the same everywhere. What the
parentheses buy you is a definite answer, not a unique expression.

That is the opening for Monday: logical equivalence is exactly this relation --
different formulas, same truth table. Do not name it yet; just let them notice
that two different trees came out the same.
:::

## WFFs

```{bnf}
<wff> ::= <atom>
        | ~ <wff>
        | ( <wff> ∧ <wff> )
        | ( <wff> ∨ <wff> )

<atom> ::= p | q 

```
Theorem: $\neg (p ∨ q))$ is not a WFF.

Proof:

::: notes
<!-- assume that it is! then ~<wff> must have been the last rule applied, 
and $( p v q))$ must be a <wff>. then ( <wff> v <wff> ) must have been the 
last rule applied, and $p$ and $q)$ must be <wff>. take each case separately:
$q)$ is not a <wff> because none of the rules apply, so we have both that it 
must be a wff and it is not a wff -- a contradiction, so our assumption 
is false!  

We include this as foreshadowing for a proof strategy called "proof by 
contradition."" Making arguments like this (proofs) is the substance of this course, 
but don't worry, we'll be teaching you how to do it. -->
:::



## Back to Propositions

A *proposition* is a statement that can be either **True** or **False**.

Examples:

-   $37 > 12$

-   Fewer than 5 people in this room feel sleepy.

-   There are extra-terrestrial life forms.

-   This statement is False.

-   $\underline{\hspace{10em}}$

## Translation

From English to logic and back...

\

$p$: \_\_\_\_\_\_\_\_\_\_\_ ate cereal for breakfast.

$q$: \_\_\_\_\_\_\_\_\_\_\_ brought a backpack to class.

 

-   $(p\lor q)$

-   $(p\wedge q)$

-   $\neg q$

# Logical Equivalence

## Logical Equivalence

\

Ex: Is it true that $(p \lor q) \equiv (q\lor p)$ ?

Ex: Is it true that
$((\text{Tu} \wedge \text{Th}) \wedge (\text{Sa} \vee \text{Su})) \equiv (\text{Tu} \wedge (\text{Th} \wedge (\text{Sa} \vee \text{Su})))$ ?

\

Discussion points:

-   $\equiv$ means *logically equivalent*

-   The answer had better be \_\_\_\_\_\_\_\_\_!!!

-   How can we justify our instinct?

::: notes
1.  Show same truth table
2.  Chains of equivalence

Same as saying "write a proof"

The second example is the swim pair from the parse-tree slide -- two different
trees, and they noticed the truth tables agreed. This is the name for what they
saw. The first example they will believe instantly; the second is the one that
earned the question.
:::


## Logical Equivalences

:::::: columns
::: {.column width="50%"}
[![](/images/LE.png){fig-alt="logical equivalences"}](https://en.wikipedia.org/wiki/Logical_equivalence)
:::

:::: {.column width="50%"}
::: small-font-size
We have discussed Logical Equivalence (LE) and explored an example.
The table illustrates LE that are so important, they have names!
:::

**Six cells are missing. What belongs in them?**

Observations:

-

-

-

-

-
::::
::::::

::: notes
The blanks are in Identity, Domination and Negation -- all six are **T** or
**F**, and all six follow from the $\wedge$ and $\vee$ truth tables. They do not
need any new vocabulary to fill them in.

Spend the last bullet on **Negation**: $p \vee \neg p \equiv$ **T** and
$p \wedge \neg p \equiv$ **F** are tautology and contradiction. Filling those two
cells *is* the next slide; it only supplies the names.

De Morgan is two rows further down -- flag it, since the cereal slide verifies
it.
:::

## Special Logical Equivalences

Suppose $p\equiv$ **T** .
Then $p$ is a *Tautology*.

Suppose $p\equiv$ **F** .
Then $p$ is a *Contradiction*.

## Logical Equivalence

::::::: small-font-size
"Select all the cereals with at least 4 units of protein and no more than 6 units of sugars."

:::::: columns
::: {.column width="25%"}
:::

::: {.column width="50%"}
$p$: a cereal has less than 4 units of protein

$q$: a cereal has more than 6 units of sugars
:::

::: {.column width="25%"}
:::
::::::

\

We suspect that $\neg p\wedge \neg q \equiv \neg( p\lor q)$:
:::::::

::::::: columns
::: {.column width="45%"}
| $p$ | $q$ | $\neg p\wedge \neg q$ |
|-----|-----|----------------------:|
| F   | F   |                       |
| F   | T   |                       |
| T   | F   |                       |
| T   | T   |                       |

: {tbl-colwidths="\[15,15,70\]"}
:::

::: {.column width="10%"}
:::

:::: {.column width="45%"}
::: {style="float: right;"}
| $p$ | $q$ | $\neg( p\lor q)$ |
|-----|-----|-----------------:|
| F   | F   |                  |
| F   | T   |                  |
| T   | F   |                  |
| T   | T   |                  |

: {tbl-colwidths="\[15,15,70\]"}
:::
::::
:::::::

::: notes
half the class complete the TT on the left, the other half on the right, then we all read our responses aloud!
:::


## Logical Equivalence Practice

All graded learning activities in the course will use a tool called *PrairieLearn*.

Navigate to Activity 1 and complete question 2.
Each of you will receive a different version of the problem.
Work together in pairs to solve each person's version.

{{< qrcode https://us.prairielearn.com/pl/course_instance/186238/assessment/2562335 >}}

<https://us.prairielearn.com/pl/course_instance/186238/assessment/2562335>

# Implication

## Implication

Connect operator $\rightarrow$ to a logically equivalent expression using only *and*, *or*, and *not*.

| $p$ | $q$ | $p\rightarrow q$ | $\underline{\hspace{3em}}$ |
|-----|-----|:----------------:|:--------------------------:|
| F   | F   |        T         |                            |
| F   | T   |        T         |                            |
| T   | F   |        F         |                            |
| T   | T   |        T         |                            |

-   Is this operator commutative?

-   Note: $p \rightarrow q \equiv$ \_\_\_\_\_\_\_.

-   $p \rightarrow q$ Corresponds to "if $p$ then $q$"

-   Gives us mechanism for reasoning!

::: notes
q and not p is false.
so it's NOT (q\^-p) == ???
(apply demorgan)

PL exercises on Logical equivalence involving implication?

More on implication and logical equivalence in tutorial questions
:::

# Predicates and Data

## Predicates

\

$p$: \_\_\_\_\_\_\_\_\_\_\_ ate cereal for breakfast.

\

We may want to apply the statement to many students, in which case we define a *Predicate*.

\

-   $P(x)$: $x$ ate cereal for breakfast.

-   $x$ can be *instantiated* to be a particular student, or an arbitrary student.

-   $P(\underline{\hspace{2em}} )$ is a proposition.

## Back to Cereal

{{< video https://youtu.be/CLQ0LZSnJFE?si=jHDBkjZ2OnkfQqwW width="850" height="475">}}


## Boolean Masks

Select all the cereals with at least 4 units of protein and no more than 6 units of sugars.

```{pyodide}
#| setup: true
#| exercise: ex_cereal
import pandas as pd
# import numpy as np

# Load and prepare data
cereals = pd.read_csv('https://raw.githubusercontent.com/UBC-CS/dsci220/refs/heads/main/data/cereal.csv')
# cereals = cereals.dropna()

```

```{pyodide}
#| exercise: ex_cereal
p = cereals['protein'] < 4 
q = cereals['sugars'] > 6
filtered_df = cereals[______]
filtered_df
```

```{pyodide}
#| exercise: ex_cereal
#| check: true
correct_answer = cereals[(cereals['protein'] >= 4) & (cereals['sugars'] <= 6)]

ok = (
    len(filtered_df) == len(correct_answer)
    and filtered_df.index.equals(correct_answer.index)
)

if ok:
    feedback = {
        "correct": True,
        "message": "Perfect! You correctly filtered for healthy cereal."
    }
else:
    feedback = {
        "correct": False,
        "message": "Not quite. Use both conditions: protein >= 4 and sugars <= 6."
    }

feedback  
```

::: notes
note that by doing this filtering, we are defining a predicate instantiated by each row whose truth value is evaluated.

Challenge: In pandas, test De Morgan with missing data: create a row with protein = NaN.
Show that (\~p) & (\~q) can differ from \~(p \| q) under three-valued logic; propose a fix (.fillna(False) or careful NA handling).
:::


## Words and Ideas

Grammar

Logical Equivalence

Tautology, Contradiction

Implication

Predicate

Boolean Mask
