---
date: 2025-09-30
---

## Announcements 

<style>
.small { font-size: 0.9rem; }
.tiny { font-size: 0.8rem; }
.rule { font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, "Liberation Mono", monospace; }
.card { border: 2px solid #ddd; border-radius: 12px; padding: 14px 16px; margin: 10px 0; }
.center { text-align: center; }
.two-col { display: grid; grid-template-columns: 1fr 1fr; gap: 18px; }
.badge { display:inline-block; padding:2px 8px; border:1px solid #bbb; border-radius:10px; font-size:.8rem; }
</style>

# Induction

## Last Demo

This is a tromino: 
<svg viewBox="0 0 48 48" width="48" height="48" fill="currentColor" role="img" aria-label="L tromino">
  <rect x="0"  y="24" width="23" height="23" rx="3" />
  <rect x="24" y="24" width="23" height="23" rx="3" />
  <rect x="0"  y="0"  width="23" height="23" rx="3" />
</svg>

This is a $n$ by $n$ deficient grid: 

<svg viewBox="0 0 200 200" width="240" height="240" role="img"
     aria-label="10 by 10 grid with one black square at row 6, column 6">
  <defs>
    <pattern id="grid20" width="20" height="20" patternUnits="userSpaceOnUse">
      <path d="M20 0H0V20" fill="none" stroke="#bbb" stroke-width="1"/>
    </pattern>
  </defs>
  <rect x="0" y="0" width="200" height="200" fill="white" stroke="#333" stroke-width="2"/>
  <rect x="0" y="0" width="200" height="200" fill="url(#grid20)"/>
  <rect x="100" y="100" width="20" height="20" fill="black"/>
</svg>

\

Claim: Every $2^n$ by $2^n$ deficient grid can be tiled with trominos.

## Tile Work

Claim: Every $2^n$ by $2^n$ deficient grid can be tiled with trominos.

\ 

Proof: 
Line 1:

\

Line 2: 

\ 

Small case(s):


## Tile Work {.smaller}

Claim: Every $2^n$ by $2^n$ deficient grid can be tiled with trominos.

\ 

Proof continued...
Inductive case:

<svg viewBox="0 0 400 400" width="480" height="480" role="img"
     aria-label="16 by 16 grid with one black square at row 4, column 12">
  <defs>
    <pattern id="grid25" width="25" height="25" patternUnits="userSpaceOnUse">
      <path d="M25 0H0V25" fill="none" stroke="#bbb" stroke-width="1"/>
    </pattern>
  </defs>
  <rect x="0" y="0" width="400" height="400" fill="white" stroke="#333" stroke-width="2"/>
  <rect x="0" y="0" width="400" height="400" fill="url(#grid25)"/>
  <rect x="300" y="150" width="25" height="25" fill="black"/>
</svg>

# Summations

## Trick 5: Reindexing 

\ 

**Story.** You only log every 3rd data point (rows 3, 6, 9, …).  
The cost for row $i$ is $2i+1$.  

**Question.** What is the total cost up to row $n$?

## Trick 6: Double Sum 

**Story.** Each item $i$ must be compared with all *later* items $j\ge i$.  The pairwise cost is $c_j = 4j+1$ (depends only on the later index).

::::: columns
::: {.column}
**Problem.**  
$T = \displaystyle \sum_{i=1}^{n}\;\sum_{j=i}^{n}\,(4j+1)$
:::
::: {.column}
![](/images/grid12.png)
:::
::::: 


# Sequences

## Hex Flowers

Consider the sequence of images below. How many cells are in image $n$? (Call it $C_n$)

![](/images/hexflowers.png)


$C_0 =$

$C_1 =$

$C_2 =$

$\ldots$

$C_n =$

## Hex Flowers

$C_0 = 1$, and 
$C_n = C_{n-1} + 6n, \forall n > 0$

But what does this function *mean*? We need a closed form!!


## Solving Recurrences

<div class="card">

1. "unroll" the recurrence by substituting previous expressions.

2. See a pattern, and generalize it using $k$.

3. Set the generalized term equal to the base case term. 

4. Substitute base case and solve sums. 

</div>

::: notes
go back and put these numbers onto the solution
:::

## T


Consider the sequence of images below. How many dots are in image $n$? (Call it $T_n$)

![](/images/recursiveT.png)


$T_0 =$

$T_1 =$

$T_2 =$

$\ldots$

$T_n =$

## T

$T_0 = 2$, and 
$T_n = T_{n-1} + 3, \forall n > 0$

But what does this function *mean*? We need a closed form!!

## T

But how can we be sure our closed form is correct?

Claim: The closed form solution to the recurrence $T_0 = 2$ and $T_n = T_{n-1} + 3, \forall n > 0$ is $T_n = 2 + 3n$

::: notes
Consider an arbitrary $n$ and the given recurrence.

IH: Assume $\forall j < n, T_j = 2 + 3j$.

Note that either $n = 0$ (base case) or $n > 0$ inductive case.

Case 1: $(n=0)$ The recurrence gives $T_0 = 2$, and the closed form gives $T_0 = 3\cdot 0 + 2 = 2$

Case 2: $(n > 0)$ The recurrence gives $T_n = T_{n-1} + 3$. But from the IH, we know $T_{n-1} = 3(n-1) + 2$. We substitute to get $T_n= [3(n-1) + 2] + 3 = 3n+2$. 
:::



## Toothpicks {.activity}

How many toothpicks in an $n\cross n$ grid made of toothpicks?

\

Draw the sequence:

\ 

Find the recurrence:

\ 

Solve the closed form for the recurrence:

## Buds

