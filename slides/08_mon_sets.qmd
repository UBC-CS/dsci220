---
date: 2025-10-19
---

## Announcements

<style>
.small { font-size: .9rem; }
.tiny { font-size: .8rem; }
.card { border: 2px solid #ddd; border-radius: 12px; padding: 14px 16px; margin: 10px 0; }
.center { text-align:center; }
</style>

# Sets 

## Sets = Filters on Rows {.smaller}

\ 

Let $U$ be **all rows** of a dataframe `df`.

- A **set** $A\subseteq U$ selects rows by a **predicate** $P_A(x)$.
- In code, a set is a **Boolean mask**: `A_mask = P_A(row)`.

\ 

**Key Ideas**

- **Membership:** $x\in A \iff P_A(x)\iff$  row mask on $x$ is `True`
- **Cardinality:** $|A| = \sum_{x\in U} \mathbf{1}_A(x)\iff$ `A_mask.sum()`
- **Subset:** $A\subseteq B \iff \forall x, P_A(x)\Rightarrow P_B(x)\iff$ `((A & ~B).sum()==0)`

::: notes
Say: “We’ll reason with three parallel languages: math sets, logical predicates, and dataframe masks. They’re equivalent.”
:::

## Math ⇄ Predicate ⇄ Mask {.smaller}

Let $U$ be all rows in `df`.

- **Math (set):** a subset $A\subseteq U$  
- **Predicate:** $A=\{\,x\in U \mid P_A(x)\,\}$  
- **Dataframe:** `A_mask = P_A(row)` then `A_df = df[A_mask]`

**We’ll use these café predicates:**

- $A=$ “Iced drinks”  
  $A=\{x\in U \mid x.\mathrm{is\_iced}=\text{True}\}$  
  `A = df['is_iced']`

- $B=$ “Non-dairy milk (Oat or Almond)”  
  $B=\{x\in U \mid x.\mathrm{milk}\in\{\text{'Oat','Almond'}\}\}$  
  `B = df['milk'].str.strip().isin(['Oat','Almond'])`

- $C=$ “High caffeine (≥150 mg)”  
  $C=\{x\in U \mid x.\mathrm{caffeine\_mg}\ge 150\}$  
  `C = df['caffeine_mg'] >= 150`

- $D=$ “Low calorie (≤150 cal)”  
  $D=\{x\in U \mid x.\mathrm{calories} \le 150\}$  
  `D = df['calories'] <= 150`

## Operations: Three Ways to Say the Same Thing {.smaller}

Let $A,B\subseteq U$.

| Operation | Math (set) | Predicate (logic) | Dataframe (mask) |
|---|---|---|---|
| **Union** | $A\cup B$ | $\{x: P_A(x)\lor P_B(x)\}$ | `(A | B)` |
| **Intersection** | $A\cap B$ | $\{x: P_A(x)\land P_B(x)\}$ | `(A & B)` |
| **Difference** | $A-B$ | $\{x: P_A(x)\land \neg P_B(x)\}$ | `(A & ~B)` |
| **Complement** | $A^c$ | $\{x: \neg P_A(x)\}$ | `~A` |
| **Symm. diff.** | $A\triangle B$ | $\{x:(P_A\lor P_B)\land \neg(P_A\land P_B)\}$ | `A ^ B` |

**Precedence note:** use `&`, `|`, `~` with **parentheses**: `(A & B) | (~C)`.

## Union Example {.smaller}

Let  
$A=\{x\mid x.\mathrm{is\_iced}\}$ and  
$B=\{x\mid x.\mathrm{milk}\in\{\text{Oat, Almond}\}\}$.

- **Math:** $A\cup B$  
- **Predicate:** $\{\,x\in U \mid x.\mathrm{is\_iced}\ \lor\ x.\mathrm{milk}\in\{\text{Oat, Almond}\}\,\}$  
- **Mask:** `(df['is_iced']) | (df['milk'].str.strip().isin(['Oat','Almond']))`

## Café Orders {.smaller}

```{pyodide}
import pandas as pd
df = pd.read_csv('https://raw.githubusercontent.com/UBC-CS/dsci220/refs/heads/main/data/cafe_orders.csv')

A = df['is_iced']
B = df['milk'].str.strip().isin(['Oat','Almond'])
C = df['caffeine_mg'] >= 150
D = df['calories'] <= 150
```

## Cardinality

```{pyodide}

# Find the cardinalities of the following sets: 
# A, B, C, D, A U B, A\cap B, A-B, (A U B)^C 

nA = A.sum()
print(nA)

```

## Identity

Experiment using the code below, and then complete the observation:

For any sets $A$, and $B$:
\_\_\_\_\_\_\_\_\_\_\_ = \_\_\_\_\_\_\_\_\_\_\_

```{pyodide}

# Find the data frames corresponding to the following sets: 
# (A U B)^c, (A^c \cap B^c)


```

## Inclusion-Exclusion

Experiment using the code below, and then complete the observation:

For any sets $A$ and $B$: $|A\cup B| =$ \_\_\_\_\_\_\_\_\_\_\_\_\_

```{pyodide}

lhs = (A | B).sum()  
try = A.sum() + B.sum()

```

## Equality?

Two sets are equal if they contain exactly the same elements. Think of a way to test set equality using set operations. 

Are sets $X$ and $Y$ equal? 

```{pyodide}

X = (df['caffeine_mg'] >= 150)
Y = df['drink'].str.contains('Americano|Drip|Cold|Iced|Tea|Espresso', case=False, regex=True)

```

## Subsets {.smaller}

$A\subseteq B \iff \forall x, P_A(x)\Rightarrow P_B(x)\iff$ `((A & ~B).sum()==0)`

Are any of our sets $A$, $B$, $C$, or $D$ subsets of one another?

```{pyodide}

((A & ~B).sum()==0)
```

## A Venn Puzzle {.smaller}

Fill in the Venn Diagram using the values for $|U|$, $|A|$, $|B|$, $|A\cup B|$. Verify your counts are correct by finding $|A-B|$, $|A\cap B|$, $|B-A|$, and $|U-(A\cup B)|$, via code.

<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 500" width="30%" role="img" aria-label="Two-set Venn diagram for sets A and B">
  <rect x="10" y="10" width="780" height="480" rx="16" ry="16" fill="#ffffff" stroke="#333333" stroke-width="2"/>
  <text x="26" y="40" fill="#111111" font-size="32" font-weight="600" font-family="ui-sans-serif, system-ui, -apple-system, Segoe UI, Arial">U</text>
  <text x="300" y="90" text-anchor="middle" fill="#111111" font-size="32" font-weight="600" font-family="ui-sans-serif, system-ui, -apple-system, Segoe UI, Arial">A</text>
  <text x="500" y="90" text-anchor="middle" fill="#111111" font-size="32" font-weight="600" font-family="ui-sans-serif, system-ui, -apple-system, Segoe UI, Arial">B</text>
  <circle cx="300" cy="260" r="180" fill="#5B8DEF" fill-opacity="0.28" stroke="#2b4ea2" stroke-width="2"/>
  <circle cx="500" cy="260" r="180" fill="#FF8A65" fill-opacity="0.28" stroke="#bf4e2c" stroke-width="2"/>
  <text x="230" y="265" text-anchor="middle" fill="#222222" font-size="24" font-family="ui-sans-serif, system-ui, -apple-system, Segoe UI, Arial">A - B</text>
  <text x="400" y="265" text-anchor="middle" fill="#222222" font-size="24" font-family="ui-sans-serif, system-ui, -apple-system, Segoe UI, Arial">A ∩ B</text>
  <text x="570" y="265" text-anchor="middle" fill="#222222" font-size="24" font-family="ui-sans-serif, system-ui, -apple-system, Segoe UI, Arial">B - A</text>
  <text x="60"  y="455"               fill="#555555" font-size="24" font-family="ui-sans-serif, system-ui, -apple-system, Segoe UI, Arial">U - (A ∪ B)</text>
</svg>

```{pyodide}

nAminusB=(A & (~B)).sum()
print(nAminusB)
```

## Bounds on $|A\cap B|$

Give upper and lower bounds on $|A\cap B|$ for any $A,B\subseteq U$. 

\ 

\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_ $\leq |A\cap B|\leq$ \_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_

\ 

<center>
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 500" width="60%" role="img" aria-label="Two-set Venn diagram for sets A and B">
  <rect x="10" y="10" width="780" height="480" rx="16" ry="16" fill="#ffffff" stroke="#333333" stroke-width="2"/>
  <text x="26" y="40" fill="#111111" font-size="32" font-weight="600" font-family="ui-sans-serif, system-ui, -apple-system, Segoe UI, Arial">U</text>
  <text x="300" y="90" text-anchor="middle" fill="#111111" font-size="32" font-weight="600" font-family="ui-sans-serif, system-ui, -apple-system, Segoe UI, Arial">A</text>
  <text x="500" y="90" text-anchor="middle" fill="#111111" font-size="32" font-weight="600" font-family="ui-sans-serif, system-ui, -apple-system, Segoe UI, Arial">B</text>
  <circle cx="300" cy="260" r="180" fill="#5B8DEF" fill-opacity="0.28" stroke="#2b4ea2" stroke-width="2"/>
  <circle cx="500" cy="260" r="180" fill="#FF8A65" fill-opacity="0.28" stroke="#bf4e2c" stroke-width="2"/>
  <text x="230" y="265" text-anchor="middle" fill="#222222" font-size="24" font-family="ui-sans-serif, system-ui, -apple-system, Segoe UI, Arial">A - B</text>
  <text x="400" y="265" text-anchor="middle" fill="#222222" font-size="24" font-family="ui-sans-serif, system-ui, -apple-system, Segoe UI, Arial">A ∩ B</text>
  <text x="570" y="265" text-anchor="middle" fill="#222222" font-size="24" font-family="ui-sans-serif, system-ui, -apple-system, Segoe UI, Arial">B - A</text>
  <text x="60"  y="455"               fill="#555555" font-size="24" font-family="ui-sans-serif, system-ui, -apple-system, Segoe UI, Arial">U - (A ∪ B)</text>
</svg>
</center>


