---
title: "Truth and Propositions"
subtitle: "Lecture 2, {{< var course.code >}}, {{< var course.term >}}"
include-after-body:
  text: |
    <script>
      function show(id, value) {
        const el = document.getElementById(id);
        if (el) el.innerText = value;
      }
      function wireUp() {
        const b1 = document.getElementById("generateButton1");
        const b2 = document.getElementById("generateButton2");
        if (b1) b1.addEventListener("click", () =>
          show("randomNumberDisplay1", Math.floor(Math.random() * 4) + 1));
        if (b2) b2.addEventListener("click", () =>
          show("randomNumberDisplay2", Math.random() < 0.5 ? "p" : "q"));
      }
      if (document.readyState === "loading")
        document.addEventListener("DOMContentLoaded", wireUp);
      else wireUp();
    </script>
---
# Logic

## Recall...

Key observations about the table:

::::: columns
::: {.column width="30%"}

| $p$ | $q$ | $p \lor q$ |
|---|---|:--------:|
| F | F |   F    |
| F | T |   T    |
| T | F |   T    |
| T | T |   T    |

:::

::: {.column style="width: 70%; font-size: 80%;"}

*   The diagram is called a _Truth Table_
*   p and q are _variables_, each of which take on one of two _Boolean_ values, True or False (T/F, 1/0) 
*   ∨ is a _binary operator_ that implements a function (creates output) from the 2 input variables to another Boolean value. It is characterized by its output values.
*   The operator ∨ corresponds to the English word _or_. 
*   The expression p ∨ q is called a _proposition_.
    
:::
:::::

## Foreshadowing {.activity}

::::: {.columns}
::: {.column width="30%"}

| $p$ | $q$ | $p \lor q$ |
|---|---|:--------:|
| F | F |   F    |
| F | T |   T    |
| T | F |   T    |
| T | T |   T    |

:::

::: {.column style="width: 70%;"}

*   How many rows for a proposition on 3 variables? $k$ variables?


*   Give a good name for an operator that takes 1 variable: __________
*   Give a good name for an operator that takes 3 variables: __________
*   How many different binary operators could there be? 
 
:::
:::::

{{< countdown minutes=6 >}}

::: notes
challenge: Of all binary operators, how many are commutative? (Half: 8.)
:::


## Truth Tables {.activity}

All graded learning activities in the course use a tool called _PrairieLearn_.

Open **Activity 1: Propositional Logic** and work through it.

{{< qrcode https://us.prairielearn.com/pl/course_instance/209866 >}}

[us.prairielearn.com/pl/course_instance/209866](https://us.prairielearn.com/pl/course_instance/209866)

{{< countdown minutes=5 >}}

::: notes
First time any of them has opened PrairieLearn. Expect enrolment friction --
walk the room. Full credit runs to Sunday, so nobody is penalised for a slow
start.
:::

## Swimming {.activity}

\ 

> I swim Tuesday and Thursday and Saturday or Sunday.

\

**How many times did I swim?**  (circle all possible values)

$$0 \qquad 1 \qquad 2 \qquad 3 \qquad 4 \qquad 5$$

{{< countdown minutes=4 >}}

::: notes
Collect before advancing.

5 is the one worth an argument: it turns on whether days I never mentioned
count. The sentence is silent about Monday.
:::

## Swimming Continued

Add parentheses so that I swam exactly this many times:

**5:**&emsp;&emsp;&emsp;$\text{Tue} \wedge \text{Thu} \wedge \text{Sat} \vee \text{Sun}$

**4:**&emsp;&emsp;&emsp;$\text{Tue} \wedge \text{Thu} \wedge \text{Sat} \vee \text{Sun}$

**3:**&emsp;&emsp;&emsp;$\text{Tue} \wedge \text{Thu} \wedge \text{Sat} \vee \text{Sun}$

**2:**&emsp;&emsp;&emsp;$\text{Tue} \wedge \text{Thu} \wedge \text{Sat} \vee \text{Sun}$

**1:**&emsp;&emsp;&emsp;$\text{Tue} \wedge \text{Thu} \wedge \text{Sat} \vee \text{Sun}$

**0:**&emsp;&emsp;&emsp;$\text{Tue} \wedge \text{Thu} \wedge \text{Sat} \vee \text{Sun}$


::: notes
Same string six times. Annotate each line live.

Close on the pivot: in English the parentheses are optional, which is why the
sentence had three readings. We are about to build a notation where they are
compulsory -- every AND and OR rule in the grammar emits its own pair, so there
is nothing to leave out and nothing to guess.
:::

## WFFs

A _well-formed formula_ is a Boolean statement generated by the following rules:

```{bnf}
<wff> ::= <atom>
        | ~ <wff>
        | ( <wff> ∧ <wff> )
        | ( <wff> ∨ <wff> )

<atom> ::= p | q | r | s | ...

```

This definition is in the form of a _Grammar_. 

_____________: `<wff>` and `<atom>`

_____________: `p`, `q`, `r`, `s` ...

It is our first self-referential or _recursive_ definition. 

## WFFs

[Rand rule]{#generateButton1 .btn .btn-primary role="button"} &nbsp; apply rule on line [&mdash;]{#randomNumberDisplay1 .rand-out}

[Rand atom]{#generateButton2 .btn .btn-primary role="button"} &nbsp; atom becomes [&mdash;]{#randomNumberDisplay2 .rand-out}

```{bnf}
<wff> ::= <atom>
        | ~ <wff>
        | ( <wff> ∧ <wff> )
        | ( <wff> ∨ <wff> )

<atom> ::= p | q 

```
Generate a WFF:

`<wff>`

## WFFs (Notes)

```{bnf}
<wff> ::= <atom>
        | ~ <wff>
        | ( <wff> ∧ <wff> )
        | ( <wff> ∨ <wff> )

<atom> ::= p | q 

```

* Grammars can be used to construct _many_ different kinds of sequences. 

* We could have included additional operators $\rightarrow$, $\leftrightarrow$, $\oplus$, $\uparrow$

* Computational evaluation of `<wff>`is covered in DSCI221. For now, we trust Python and focus on logic.

* The `<wff>` are _propositions_. 

## WFF Puzzle {.activity}

Which of these are WFFs?


```{bnf}
<wff> ::= <atom>
        | ~ <wff>
        | ( <wff> ∧ <wff> )
        | ( <wff> ∨ <wff> )

<atom> ::= p | q 

```

::::: {.columns}
::: {.column width="50%"}

1. $(\neg p ∧ (q ∨ r))$  
2. $((p ∧ q) ∨ (r ∧ \neg s))$ 
3. $((\neg p) ∨ (q ∨ r))$ 

:::
::: {.column width="50%"}

4. $(p ∨ (qr))$
5. $\neg (p ∨ (q ∧ \neg r))$
6. $\neg (p ∨ q))$

:::
:::::

{{< countdown minutes=3 >}}


::: notes
<!-- notes: sometimes pq is the same as p^q, p & q, p && q, but our grammar only 
uses ^. In this course we will vary symbols fluidly, partly because we're 
assembling info from many different sources, but also partly because you'll
need to be agile in the future. Note that the same issues exist for other operators. -->
:::

## WFFs

Build the parse tree for

$$((\text{Tu} \wedge \text{Th}) \wedge (\text{Sa} \vee \text{Su}))$$

```{bnf}
<wff> ::= <atom>
        | ~ <wff>
        | ( <wff> ∧ <wff> )
        | ( <wff> ∨ <wff> )

<atom> ::= Tu | Th | Sa | Su

```

\

\

::: notes
The correct parenthesization of the swim sentence, drawn as a tree. Work
outward-in: the outermost pair of parentheses is the last rule applied, so it is
the root.

Each internal node is a rule application; each leaf is an atom. The tree records
which rule was used where -- which is what the parentheses were encoding all
along, and why dropping them loses information.
:::

## WFFs

```{bnf}
<wff> ::= <atom>
        | ~ <wff>
        | ( <wff> ∧ <wff> )
        | ( <wff> ∨ <wff> )

<atom> ::= p | q 

```
Theorem: $\neg (p ∨ q))$ is not a WFF.

Proof:

::: notes
<!-- assume that it is! then ~<wff> must have been the last rule applied, 
and $( p v q))$ must be a <wff>. then ( <wff> v <wff> ) must have been the 
last rule applied, and $p$ and $q)$ must be <wff>. take each case separately:
$q)$ is not a <wff> because none of the rules apply, so we have both that it 
must be a wff and it is not a wff -- a contradiction, so our assumption 
is false!  

We include this as foreshadowing for a proof strategy called "proof by 
contradition."" Making arguments like this (proofs) is the substance of this course, 
but don't worry, we'll be teaching you how to do it. -->
:::

## Words and Ideas

Truth Table

Proposition

Grammar

Well-formed formula

Recursive definition
